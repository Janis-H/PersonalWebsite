import React from 'react';

import NavButton from '../components/NavButton.jsx';

const Navbar = () => {
  return (
    <nav id="navbar">
      <NavButton name="Home"/>
    </nav>
  );
};

export default Navbar;
